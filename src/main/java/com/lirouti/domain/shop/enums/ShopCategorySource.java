package com.lirouti.domain.shop.enums;

/**
 * 탭을 눌렀을 때 무엇을 가져와야 하는가.
 *
 * <p><b>지금은 값이 하나뿐이다.</b> 캐릭터 탭을 걷어내면서 아이템이 아닌 원천이 사라졌다.
 *
 * <p>그럼에도 이 필드를 응답에서 빼지 않는다. 이 API 의 존재 이유가 <b>탭이 늘거나 줄 때 앱
 * 배포 없이 따라오게</b> 하는 것인데, 앱이 이 값으로 "무엇을 부를지" 를 정하고 있다. 지금
 * 빼면 그 분기가 무너지고, 나중에 아이템 아닌 탭이 다시 생길 때 앱을 또 고쳐야 한다 — 값이
 * 하나인 동안의 비용은 필드 하나뿐이다.
 */
public enum ShopCategorySource {

    /** 아바타 아이템 목록에서 가져온다. 슬롯이 있으면 그 슬롯으로 거르고, 없으면 전부. */
    ITEM
}
