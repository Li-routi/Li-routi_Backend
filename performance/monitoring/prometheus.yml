global:
  scrape_interval: 5s
  scrape_timeout: 4s

scrape_configs:
  - job_name: lirouti
    metrics_path: /actuator/prometheus
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/secrets/actuator-token
    static_configs:
      - targets:
          - host.docker.internal:8080
